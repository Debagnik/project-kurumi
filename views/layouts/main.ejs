<!DOCTYPE html>
<html lang="en">
    <head>
        <%= config.googleAnalyticsScript ? config.googleAnalyticsScript.replace(/^["']|["']$/g, '') : '' %>
        <%= config.inspectletScript ? config.inspectletScript.replace(/^["']|["']$/g, '') : '' %>         
        <meta charset="utf-8"/>
        <meta http-equiv="content-script-type" content="text/html"/>
        <meta http-equiv="content-script-type" content="text/javascript"/>
        <meta http-equiv="content-script-type" content="text/css"/>
        <meta http-equiv="content-type" content="text/html; charset=UTF-8"/>
        <meta name="keywords" content="<%= (config.siteMetaDataKeywords ? `${config.siteMetaDataKeywords}, ${[locals.keywords, locals.title].filter(Boolean).join(', ')}` : [locals.keywords, locals.title, locals.description].filter(Boolean).join(', ') || 'Project Walnut') %>"/>
        <meta name="description" content="<%= (config.siteMetaDataDescription ? `${config.siteMetaDataDescription}, ${[locals.description].filter(Boolean).join(', ')}` : [locals.description].filter(Boolean).join(', ') || 'A random description as a placeholder') %>"/>
        <meta name="author" content="<%= (config.siteMetaDataAuthor ? `${config.siteMetaDataAuthor}` : 'Debagnik Kar' )%>"/>
        <meta http-equiv="x-ua-compatible" content="ie=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
        <meta name="robots" content="index, follow">
        <meta name="revisit-after" content="1 days">
        <meta name="msapplication-TileColor" content="#da532c">
        <meta name="theme-color" content="#000000">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <link rel="stylesheet" href="/css/style.css">
        <script type="text/javascript" defer src="/js/script.js"></script>
        <title><%= config.siteName %></title>
    </head>
<body>
    <%- include('../partials/search.ejs') %>
        <div class="container">
            <%- include('../partials/header.ejs') %>
                <main class="main">
                    <%- body %>
                </main>
                <%- include('../partials/footer.ejs') %>
        </div>

</body>

</html>